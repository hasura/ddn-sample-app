kind: Connector
version: v2
definition:
  name: pg
  subgraph: sales
  source: hasura/postgres:v0.7.0
  context: .
  envFile: .env.cloud
  envMapping:
    CONNECTION_URI:
      fromEnv: CONNECTION_URI
