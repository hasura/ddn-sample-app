kind: Subgraph
version: v1
definition:
  generator:
    rootPath: .
  envFile: .env.supergraph_globals
  includePaths:
    - auth-config.hml
    - compatibility-config.hml
    - graphql-config.hml
  name: globals
