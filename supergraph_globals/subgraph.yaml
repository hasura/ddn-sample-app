kind: Subgraph
version: v2
definition:
  name: globals
  generator:
    rootPath: .
  envFile: .env.supergraph_globals
  includePaths:
    - auth-config.hml
    - compatibility-config.hml
    - graphql-config.hml
