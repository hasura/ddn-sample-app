kind: Subgraph
version: v2
definition:
  name: analytics
  generator:
    rootPath: .
  envFile: .env.cloud.analytics
  includePaths:
    - metadata
  envMapping:
    ANALYTICS_CLICKHOUSE_READ_URL:
      fromEnv: ANALYTICS_CLICKHOUSE_READ_URL
    ANALYTICS_CLICKHOUSE_WRITE_URL:
      fromEnv: ANALYTICS_CLICKHOUSE_WRITE_URL
  connectors:
    - path: connector/clickhouse/connector.cloud.yaml
      connectorLinkName: clickhouse
